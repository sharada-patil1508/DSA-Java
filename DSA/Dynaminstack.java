package DSA;
import java.util.*;
class Dynamicstacks extends Customestack{
	
	/* public Dynamicstacks() {
         super();
     }

     public Dynamicstacks(int size) {
         super(size);
     }

     @Override
     public boolean push(int item) {
         // this will take care of it being full
         if (this.isFull()) {
             // if custom stack is full then double the size
             int[] temp = new int[data.length * 2];

             // copy all previous items into new data
             for (int i = 0; i < data.length; i++) {
                 temp[i] = data[i];
             }
             data = temp;
         }

         // at this point we know that the array is not full
         // insert item normally by calling Customestack push function
         return super.push(item);*/

}

